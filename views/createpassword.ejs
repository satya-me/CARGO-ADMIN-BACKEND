<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>CargoBooking Admin & Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.2/css/all.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- <link rel="stylesheet" href="assets/css/themes.css"> -->
    <style>
        .bg-primary,
        .btn-primary {
            background: #71a311 !important;
        }

        .btn {
            outline: 0;
            border: 0;
        }
    </style>
</head>

<body>
    <div class="account-pages mt-5">
        <div class="auth-logo text-center">
            <a href="#" class="auth-logo-dark">


                <img src="/public/images/logo (3).png" alt="" height="50">
            </a>
        </div>
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6 col-xl-5">
                    <div class="card overflow-hidden">
                        <div class="bg-primary">
                            <div class="row">
                                <div class="col-7">
                                    <div class="text-white p-4">
                                        <h5 class="text-white">Create Password !</h5>
                                        <p>Admin.</p>
                                    </div>
                                </div>
                                <div class="col-5 align-self-end">
                                    <img src="/public/images/profile-img3.png" alt="" class="img-fluid">
                                </div>
                            </div>
                        </div>
                        <div class="card-body pt-0">

                            <div class="p-2">
                                <form class="form-horizontal" action="/">
                                    <input type="hidden" id="dataFromController" value="<%=data%>">
                                    <div class="mb-3">
                                        <label class="form-label">Create Password</label>
                                        <input type="password" class="form-control" placeholder="Enter password"
                                            aria-label="Password" aria-describedby="password-addon">
                                    </div>

                                    <div class="mb-3">
                                        <label class="form-label">Re-Type Password</label>
                                        <input type="password" class="form-control" placeholder="Enter password"
                                            aria-label="Password" aria-describedby="password-addon">
                                    </div>



                                    <div class="mt-3 d-grid">
                                        <button class="btn btn-primary btn-block">Create
                                            Password</button>

                                    </div>


                                </form>
                            </div>

                        </div>
                    </div>
                    <div class="mt-3 text-center">

                        <div>
                            <p>©
                                <script>
                                    document.write(new Date().getFullYear())
                                </script> CargoBooking. all rights reserved.
                            </p>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <!-- <script>
        // Retrieve the form element
        const form = document.querySelector('form');

        // Add submit event listener to the form
        form.addEventListener('submit', (e) => {
            const data = document.getElementById('dataFromController').value;
            console.log("data");
            e.preventDefault(); // Prevent the default form submission

            // Retrieve the input values
            const password = document.querySelector('input[name="password"]').value;
            const retypePassword = document.querySelector('input[name="retypePassword"]').value;

            // Create an object with the form data
            const formData = {
                password: password,
                retypePassword: retypePassword
            };
            // const url = "http://192.168.1.23:3304/api/admin/auth/setpassword/" + data?.email + "/" + data?.token;
            // Send a POST request to the server
            fetch("http://192.168.1.23:3304/api/admin/auth/setpassword/" + data?.email + "/" + data?.token, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(formData?.password)
            })
                .then(response => response.json())
                .then(data => {
                    // Handle the response from the server
                    console.log(data);
                    // You can perform further actions here based on the response
                })
                .catch(error => {
                    // Handle any errors that occur during the request
                    console.error('Error:', error);
                });
        });

    </script> -->

    <!-- modal -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
</body>

</html>